# MongoDB Atlas connection string — keep this secret, never commit .env
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority

# Port the Express server listens on (defaults to 8000)
PORT=8000
